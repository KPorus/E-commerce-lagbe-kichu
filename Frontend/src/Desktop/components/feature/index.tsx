import SectionTitle from '@/Desktop/common/section-title';
import { Container } from '@chakra-ui/react';
import React from 'react';

const Feature = () => {
    return (
        <Container maxW="breakpoint-xl">
            <SectionTitle title="Features Products" />
        </Container>
    );
};

export default Feature;